# 3D Rendering Demo

A small Windows C++ rasterizer demo that renders a spinning cube using a simple software renderer.

## Files
- `rendering.cpp` - Main rendering code

## Build
Compile with a C++ compiler on Windows, for example:

```bat
build.bat
```


